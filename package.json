{
	"name": "testing-cap-cds",
	"version": "1.0.0",
	"description": "Generated by cds-services-archetype",
	"license": "ISC",
	"repository": "<Add your repository here>",
	"scripts": {
		"dev:build": "cds build && npm run dev:db-sync",
		"dev:db-sync": "cds deploy --to sqlite:my.db",
		"prod:build": "cds build --production"
	},
	"dependencies": {
		"@sap/cds": "^5.1.5",
		"npm-run-all": "^4.1.5"
	},
	"devDependencies": {
		"@sap/cds-dk": "^4.1.5",
		"@sap/hdi-dynamic-deploy": "latest",
		"sqlite3": "5.0.0"
	},
	"cds": {
		"hana": {
			"deploy-format": "hdbtable"
		},
		"requires": {
			"db": {
				"kind": "sqlite",
				"credentials": {
					"database": "my.db"
				}
			}
		}
	},
	"private": true
}
